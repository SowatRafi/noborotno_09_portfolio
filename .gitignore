# Dependencies and build output
node_modules/
dist/

# Local tooling state — Claude Code permissions and installed skills are
# machine-local developer tooling, not part of the website source.
.claude/

# Working notes — every markdown file except the README is local-only and
# must never appear in the repository or its history.
*.md
!README.md

# Original photo masters — only the optimised portrait.jpg ships with the
# site; committing these would publish the full-resolution files on Pages.
public/Sowat.Rafi_WEB.jpg
public/Sowat.Rafi_PRINT.jpg

# Editor / OS noise
.DS_Store
Thumbs.db
*.log
